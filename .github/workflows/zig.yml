name: test-zig
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: goto-bus-stop/setup-zig@v2
      - name: test
        run: zig build test
        working-directory: ~/Zig
    
  
